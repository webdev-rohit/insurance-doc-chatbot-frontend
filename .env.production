# Loaded automatically by `npm run build` and `npm run preview` (vite runs in
# mode=production). The built SPA calls these two Cloud Run services directly
# — there's no dev proxy in a production build.
VITE_INGESTION_API_BASE_URL=https://insurance-ingestion-591946978201.asia-south1.run.app
VITE_QUERY_API_BASE_URL=https://insurance-query-591946978201.asia-south1.run.app
